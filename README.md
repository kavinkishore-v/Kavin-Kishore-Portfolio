# Kavin Kishore Portfolio — Reliable Static Conversion

Faithful static conversion of the supplied React portfolio into HTML, compiled Tailwind CSS and vanilla JavaScript. Original assets and content are preserved.

Open `index.html`. No Node/Vite/React is required at runtime.

The background video is the original Mux HLS stream and therefore needs Internet access. The rest of the portfolio assets are local.
